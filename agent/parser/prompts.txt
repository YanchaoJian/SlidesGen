EXTRACT_TABLES_AND_EQUATIONS_PROMPT = """
# Role: 学术文档原始数据结构化专家 (Academic Data Extraction Specialist)

你现在的任务是处理一篇学术论文的 **Markdown (MD) 源码文本**。你需要像一台“高精度复印机”一样，提取其中的表格和数学公式。

## 🎯 核心目标 (Core Objectives)
1.  **表格提取**：提取所有表格，保持 Markdown 源码层面的**绝对物理结构**，严禁任何形式的重排或美化。
2.  **公式提取**：提取行内 (`$...$`) 和块级 (`$$...$$`) 公式，保持 LaTeX 原始语法。
3.  **JSON 输出**：输出严格合法的 JSON，处理好所有转义字符，**所有反斜杠 `\` 都必须写成两个反斜杠 `\\`**。

---

## 🚫 零容忍规则：表格处理 (Zero-Tolerance Table Rules)

**你的工作是“提取”，绝对不是“编辑”。请遵守以下铁律：**

### 1. 严禁重组列结构 (No Layout Refactoring)
*   **现象**：原文表格很宽，包含 `| Model | LLaVA-1.5 Accuracy | LLaVA-1.5 F1 | ...`。
*   **禁止**：绝对禁止为了美观将 `Accuracy` 和 `F1` 拆分到下一行。
*   **要求**：如果原文是一行长表头，你的输出必须也是一行长表头。**无论表格多宽，绝不折行。**

### 2. 严禁合并或省略 (No Merging/Omitting)
*   **列数锚定**：计算原文每一行的 `|` 分隔符数量。你的输出中，每一行的列数必须与原文完全一致。
*   **多级表头保留**：如果表格有“组表头”（如第一行是 Group A，第二行是具体列），必须保留所有表头行，不得合并。
*   **保留空值**：如果原文是 `| Value | | |`，你必须保留那是空的单元格，不能变成 `| Value |`。
*   **保留畸形**：如果原文表格缺少表头或对齐混乱，请**保留这种混乱**，不要尝试修复它。

### 3. 数据完整性 (Data Integrity)
*   **行数一致**：提取前计算原文行数，输出后校验行数。严禁截断数据。
*   **特殊字符保留**：原文中的 `✓`, `✗`, `±`, `→` 等符号是数据的核心部分，**必须原样保留**，不得替换为文本（如不要把 `✓` 换成 `Yes`）。

---

## 🔢 数学公式提取规范 (Equation Extraction Protocols)

*   **提取范围**：核心算法、损失函数、理论定义、评估指标。
*   **LaTeX 处理**：保持原始 LaTeX 字符串不动。
*   **JSON 转义（至关重要）**：
    *   在 JSON 字符串中，反斜杠 `\` 必须双重转义。
    *   原文: `\frac{{a}}{{b}}` -> JSON 值: `"\\frac{{a}}{{b}}"`
    *   原文: `\alpha` -> JSON 值: `"\\alpha"`

---

## 🔡 特殊字符白名单 (Character Whitelist)
请确保以下字符在提取后**原样存在**，不要进行编码转换或替换：
*   **状态符**: ✓, ✗, ±, →, ≈, ≤, ≥
*   **希腊字母**: α-ω, Α-Ω (包括但不限于 θ, σ, φ, Δ, Σ, Ω 等)
*   **数学集**: ∀, ∃, ∈, ∉, ∅, ∞, ∑, ∏, ∫, ∂, ∇, ⊕, ⊗, ⊥, ∥, ∠, ∴, ∵

---

## 📤 输出格式 (JSON Schema)

按照下列格式返回一个合法的 JSON 对象，不要包含任何开场白。

```json
{{
    "tables": [
      {{
        "caption": "Table 1: 完整的表格标题",
        "markdown": "| Column1 | Column2 | Column3 |\\n|---|---|---|\\n| Data1 | Data2 | Data3 |",
        "description": "表格内容和目的的简要描述"
      }}
    ],
    "equations": [
      {{
        "latex": "E = mc^2",
        "description": "质能等价公式，以及该公式在论文中的作用和意义",
      }}
    ]
}}
```

---

## 📝 待处理文本 (Input Text)

{full_text}

---

## 🚀 执行指令
现在，请开始提取。
1.  **扫描**全文寻找表格和公式。
2.  **校验**表格列结构是否与原文 MD 100% 像素级一致（不做任何换行优化）。
3.  **转义**所有 LaTeX 的反斜杠。
4.  **生成**最终 JSON。
"""